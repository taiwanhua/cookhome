# 接手理解考卷

受測者:對本專案**沒有任何對話記憶**的 AI(或新成員)。規則:只准依 repo 內容作答,每題附推理依據與出處;不確定就明說,禁止編造。評分由主考官對照答案卷(不在 repo 內)。

## A. 場景推理(答案不在任何單一文件,需綜合推理)

組織鏈設定(A 組通用):`根組織 > 租戶A > A-1`、`租戶A > A-2`。

1. 小明屬 A-1 與 A-2,持有「公告管理」角色(租戶A 擁有)。管理員將他自 A-1 移除,確認彈窗選「解除所有失去資格的角色」。「公告管理」會被解除嗎?為什麼?
2. 小美**只屬 A-1**,持有「公告管理(租戶A 擁有)」。將她自 A-1 移除、選「全部保留」。之後她還能使用公告管理的功能嗎?能看到什麼資料?
3. 「組織外」標籤會出現在哪些畫面?它代表什麼?為什麼被標示的角色功能仍然有效?
4. 某使用者的角色綁了權限 `demo.sample-two.view`,但角色**沒有綁**模組 `demo.sample-two`。他手打網址能進該頁嗎?為什麼?
5. 某角色勾了 `demo.*`(資料庫存幾筆?)。下一版種子為示範模組1 新增權限 `export`,這個角色需要重新設定嗎?若之後管理員在矩陣取消勾選其中的「刪除」,資料庫會發生什麼變化?
6. 超級管理員的「全域權限」在 permissions / role_permission 兩張表裡長什麼樣子?
7. 資料範圍設定了兩條規則:①套用對象=客服角色,條件「建立者=【操作者本人】」②套用對象=租戶A,條件「建立時間 之後 2026-01-01」。小華是租戶A的客服,頂層合成為 OR 時他看見什麼?改成 AND 呢?無論如何都疊加的那一層是什麼?
8. 為什麼 seed 每次部署都重跑,資料卻不會越跑越多?migrate 為什麼跑過的不會再跑?
9. 登入識別是哪個欄位?email 又是做什麼用的?為什麼兩者都必須全庫唯一?
10. 「當前組織」決定哪兩件事?它**不**決定哪兩件事?
11. 權限 key `demo.sub.sample-one.edit-page.show-history` — 哪段是動作、哪段是模組?程式要知道這筆權限屬於哪個模組時,可以解析這個字串嗎?該怎麼查?
12. 「租戶擁有者」和「持有租戶管理員角色的人」差在哪?誰有辦法停用租戶擁有者的帳號?

## B. 流程計畫題(只寫計畫,不實作)

13. 要為示範模組2 新增一筆權限 `export`,並讓已勾 `demo.*` 的角色自動擁有:你會動哪些檔案、走什麼流程?需要 migration 嗎?
14. 你接到一個新功能需求(規格已與需求方收斂完畢):描述從規格到 merge 進 main 的完整流程,以及每一步用什麼工具或 skill。
15. 你在 api 寫程式需要查 `demo_items_one` 的資料,可以直接呼叫 Mongoose 的 `Model.find` 嗎?正確做法是什麼?背後防的是什麼?
16. 你剛修改了某份 ADR 的一條規則,提交前應檢查哪些地方可能需要同步更新?
