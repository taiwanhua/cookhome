# 權限測試劇本(以示範模組驗證)

驗證對象:權限模型(ADR-0004)、查詢與判斷流程(ADR-0011)、資料範圍規則(ADR-0008)、移除所屬組織(ADR-0003)、管理範圍與可見範圍的分工(ADR-0005)、檔案儲存(ADR-0010)、表單引擎(`docs/concepts/form-engine.md`)、審核流程(`docs/modules/workflows.md`)。模組與資料定義見 [示範模組1](../modules/demo.sub.sample-one.md)、[示範模組2](../modules/demo.sample-two.md)、[購物清單](../modules/shopping-list.md)。

1. **wildcard 涵蓋未來(同層)**:角色勾示範模組1 的 `demo.sub.sample-one.*`(存單筆);下一版 seed 對示範模組1 新增權限 → 不改角色即擁有;對其編輯頁(`…edit-page`)新增的權限**不**自動擁有(子模組由自己的 `*` 代表)。
2. **資料範圍規則**:建規則「套用對象=客服角色 → 建立者 = 【操作者本人】」→ 客服查示範項目僅見自建;刪規則 → 恢復可見範圍。
3. **未宣告對照**:示範模組2 查詢僅受可見範圍保底(規則機制不介入)。
4. **頂層合成**:建兩條都命中同一人的規則,切換 OR/AND 驗結果集為聯集/交集。
5. **欄位級(綁父)**:無 `show-internal-note` → 詳情/編輯頁無此欄、API 投影排除;有 show 無 edit → 唯讀;硬送該欄寫入 → 拒。
6. **頁面自有權限**:無 `edit-page.show-history` → 編輯頁無歷程區塊,編輯功能不受影響(兩者獨立);`create-page.show-tips` 同理。
7. **路由防守**:角色未綁新增頁模組 → 手打網址被擋;綁了新增頁但無 `create` 權限 → 頁進得去、送出被 API 擋(頁與功能分離)。
8. **組織外**:授予示範模組1 的角色後移除所屬組織(radio 選保留)→ 三處列表標示、功能仍可用、資料範圍縮小。
9. **移除三檔**:dry-run 清單:(b) 只列該組織擁有 vs (c) 加列失去子樹支撐 — 用多所屬組織帳號驗差異;對象還要持有一個「擁有組織在被移除組織的**上層**、但不包住他其他所屬組織」的角色,否則 (b) / (c) 列出來一樣(見下方劇本 9)。
10. **防越權**:矩陣只可勾操作者自身擁有的權限與模組。
11. **儲存雙路**:coverPath 公開 URL 直接顯示;attachmentPath 簽名下載,無 `view` 者 API 拒發。
12. **可見性開關**:ON→OFF 驗**業務資料**的名單與資料收縮、既有角色授予不動;治理頁(組織 / 使用者 / 角色)完全不動 — 見劇本 14。
13. **總覽也是模組**:角色未綁 `overview.*` → 登入後 `/` 轉到側欄第一個能進的頁(如 `/system/org-manager`),側欄沒有「總覽」列;手打 `/overview` 顯示無權限頁;綁上 `overview.*` → 登入落在 `/overview`(ADR-0011「路由與導向規則」)。
14. **管理範圍 vs 可見範圍**(CONTEXT.md 兩個詞條;ADR-0005 的分工表):
    - **前置**:管理範圍算的是**全部**啟用中角色的擁有組織子樹(不只治理角色)。甲身上若留著擁有組織 = 租戶頂層的角色(例如「客服」),樹根永遠是租戶頂層 —— 看樹根的小節先把那種角色拿掉。
    - **由角色決定、與所屬哪裡無關**:甲的所屬組織只有「南港店」,但持有一個擁有組織 = 租戶頂層的角色 → 組織管理頁的樹根是租戶頂層、整個租戶的組織與使用者都管得到;把角色換成擁有組織 = 南港店的那一個 → 樹根變成南港店,租戶頂層與其他分店**不出現**在樹上(不是灰的,是不回傳),`org(其他分店 id)` 回 `NOT_FOUND`。
    - **不受可見性開關影響**:把該租戶的開關設成 `own` → 上一條的結果完全不變(治理頁、角色候選、搬移候選都不動);同一個人去看業務模組(示範模組1)的清單則收縮成只剩所屬組織的資料 — 兩個範圍各管各的。
    - **多根**:先讓甲也加入內湖店(授予要過資格檢查,只屬南港店會拿到 `USER_NOT_ELIGIBLE`),再同時授予擁有組織 = 南港店與 = 內湖店(兩者沒有共同上層)的兩個角色 → 組織樹有**兩個根**;停用其中一個角色 → 那一棵子樹整個退出管理範圍。
    - **無治理角色 = 空**:把全部角色解除(或全部停用)→ 沒有任何治理權限,治理頁進不去(`FORBIDDEN`)。「硬給 `view` 但管理範圍是空的」在產品上**造不出來**(權限與管理範圍來自同一批啟用中角色,給了 `view` 就一定帶著那個角色的擁有組織),只由 api 測試覆蓋。
    - **可見性開關的權限搬家**:租戶管理員(模板副本,含 `system.org-manager.*`)在編輯**自己租戶頂層**時看得到「使用者可見自身組織的下層組織資料」開關並設得了;對**別的租戶**的頂層送出 → `NOT_FOUND`(管理範圍外);對非租戶頂層送出 → `VALIDATION_FAILED`;只持 `system.org-manager.view` 的人連「編輯」都沒有;持 `view` + `edit`、沒有 `set-visibility` 的人編輯彈窗裡沒有開關,兩者硬送都是 `FORBIDDEN`。
    - **搬移候選與租戶頂層保護**:候選 = 管理範圍 ∩ 同租戶 − 自己子樹;把租戶內的組織搬去別的租戶 → 範圍外即 `NOT_FOUND`(根組織看得到兩邊才會拿到 `CROSS_TENANT`),搬進自己的子樹 → `CYCLIC_MOVE`;租戶內的人搬租戶頂層本身 → `FORBIDDEN`。
15. **側欄商標繼承**(ADR-0010):租戶頂層設商標、下層不設 → 下層使用者的側欄顯示租戶的商標;下層自己設一張 → 改顯示自己的;把下層的清掉 → 又回到繼承來的那張(沿 `ancestors` 由近到遠找第一個有商標的上層)。
16. **租戶視角**:root 開通租戶(取消勾選示範群組)→ 租戶管理員登入:側欄無「模組與權限」「資料範圍」「示範群組」、組織樹以租戶頂層為根、沒有「開通租戶」按鈕;root 視角看得到新租戶並標「租戶」。
17. **擁有者保護**:租戶管理員試圖停用 / 移出租戶 / 解除自己(擁有者)的租戶管理員授予 → `OWNER_PROTECTED`;root 可執行;租戶內任何人對租戶頂層停用 / 刪除 / 搬移 → `FORBIDDEN`。
18. **表單生命週期**:root 在表單管理建共用表單 → 設計 → 存草稿 → 發布 → 分派給租戶A → +user 在購物清單新增、填寫、送出 → 列表看得到那一筆、詳情看得到填的值。
19. **客製副本與退役**:+tenant 以分派來的表單為基底建客製表單 → 改欄位 → 發布 → +user 新增時可以選它 → +tenant 退役目前版本 → +user 不能再以它新增(已送出的照常看)。
20. **兩級核准與主管解析**:申請人在沒有主管的下層組織送出 → 往上找到上層組織的主管 → 主管核准 → 人資(角色來源)核准 → 已完成。
21. **退回修改 → 再送 → 駁回**:主管退回(理由必填)→ 申請人改完再送(修訂 +1、從第一關重審)→ 主管駁回 → 已駁回、不能再改。
22. **審核者停用 → 阻擋 → 改派**:審核者被停用 → 單被阻擋 → 流程管理者在阻擋清單改派給另一人 → 新審核者核准 → 已完成。
23. **核准後作廢 → 複製為新單 → 重審**:綁流程的已完成單沒有「編輯」只有「作廢」→ 作廢(理由必填)→ 複製為新單 → 送出重新審核。
24. **三部門平行**:初審後財務 / 法務 / 採購三條分支同時審;採購那條因承辦人停用而阻擋、改派,財務與法務照審;三條都通過才匯合,原部門確認核定。

---

# 怎麼驗:每條劇本用哪一頁、哪個帳號、什麼步驟

示範模組、購物清單、請假與申請中心都有真頁面,24 條劇本**全部可以在畫面上跑完**(唯一的例外是劇本 1 的跨版本那半條,見該節)。

**寫或改一條劇本的前置 / 預期之前,先對一次規則正本**:劇本 8 / 9 / 12 / 14 / 17 最容易寫成「照字面做驗不出來」,要對齊的規則是 —— 可見範圍是**所有**所屬組織的聯集(ADR-0005)、最後一個所屬組織不可移除(`LAST_ORG`)且檢查在擁有者保護之前、管理範圍算**全部**啟用中角色、授予要過資格檢查(ADR-0003)。E2E 的 spec 開頭註解與本文件的前置一致;**畫面文案一律從 zh-TW 字典(`packages/i18n/messages/zh-TW/admin.json`)原樣複製**,不要憑印象寫(「保留所有角色授予」不是「全部保留」)。

## 驗收前的準備

### 三個帳號

| 代號        | 是誰                                                                                                                                            | 怎麼來                                                                                                                                                                                                                                                                                                                                         |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **root**    | seed 的超級管理員(帳號 `root`,屬根組織;ADR-0002)                                                                                                | 部署時由 seed 建立,密碼在該環境的 Secret(`ROOT_ADMIN_*`,見 `docs/env-registry.md`)                                                                                                                                                                                                                                                             |
| **+tenant** | 租戶A 的首任租戶管理員 = **租戶擁有者**,持「租戶管理員」角色(角色清單上掛「預設角色」標籤,找角色時認「租戶管理員」這個名字);所屬組織 = 租戶頂層 | root 在組織管理頁按「開通租戶」建出租戶A 與這個帳號(劇本 16 就是在驗這一步)。**密碼不是誰給的,是本人從啟用信的連結自己設的**(ADR-0009,7 天效期):dev / staging 的信會寄到 `MAIL_ALLOWLIST` 上的開發者信箱;**本機與 e2e 沒有 `RESEND_API_KEY`,api 自動改用記錄用 adapter,啟用連結直接印在 api 的 stdout**(`docs/env-registry.md`),從那裡複製即可 |
| **+user**   | 租戶A 底下一般使用者,所屬組織 = 南港店,持租戶自建的「客服」角色                                                                                 | +tenant 在使用者管理頁建立,再於角色管理頁把「客服」指派給他                                                                                                                                                                                                                                                                                    |

> **根組織視角下每個租戶都有同名的角色**(例如每個租戶各建一個「客服」):root 在資料範圍頁挑「套用對象 = 指定角色」時,下拉裡會出現好幾個「客服」,**靠次文字(擁有組織)分辨**,不要只看主文字(`docs/modules/role-manager.md`「角色選單怎麼分辨同名角色」)。挑錯租戶的同名角色,規則會命中不到任何人,看起來像規則沒生效。

### 組織與角色的前置

1. **root** 登入 → 組織管理 → 開通租戶,建「租戶A」與 **+tenant**(劇本 16 一併驗)。
2. **+tenant** 登入 → 組織管理 → 在租戶A 底下建「南港店」「內湖店」兩個分店。
3. **+tenant** → 使用者管理 → 建 **+user**(所屬組織:南港店);順手把 **+tenant 自己也加入南港店**(多一個所屬組織)——劇本 2 / 4 要兩個帳號的可見範圍有交集才比得出差異;劇本 17 也靠它(+tenant 只屬租戶頂層的話,「移出租戶」會先撞 `LAST_ORG`)。劇本 12 反而要在資料建完後把他移出南港店,見該節。
   **「加進某個組織」有兩個入口**:組織管理 → 選「南港店」→「成員」分頁 →「加入成員」,一次可多選。兩個入口做的是同一件事(對每個人的所屬組織加一筆、寫同一筆 `user.add-org` 稽核),挑順手的用;**移出**只有使用者管理那一個入口。
4. **+tenant** → 角色管理 → 建「客服」角色(擁有組織 = 租戶A),權限矩陣勾示範家族(示範群組 → 示範次群組 → 示範模組1 與其三個隱藏頁、示範模組2 與其三個隱藏頁),各給 `view` / `create` / `edit` / `delete`;**先不要勾** `show-internal-note` / `edit-internal-note` / `show-tips` / `show-history`(劇本 5 / 6 會一項一項打開)。指派給 +user。

### 示範資料的兩個已知限制(會影響步驟,不是 bug)

- **seed 的 10 筆示範資料全部落在根組織**(`apps/db-migrator/seeds/demo-items.ts`;`orgs` 種子只有根組織一筆,租戶是開通出來的、各環境 id 不同,seed 引用不到)。所以 **+tenant / +user 一登入,兩支示範模組的列表都是空的** —— 那是對的,不是壞掉。凡是要比對「看得到 / 看不到」的劇本(2 / 3 / 4 / 8 / 12),**都要先用租戶帳號自己新增幾筆**。
- **seed 資料的建立者是固定的假 id**,所以 root 看 seed 那 10 筆時,詳情頁的「建立者」一律顯示「—」。要在畫面上看到人名,就自己新增一筆。

### 劇本 2 / 12 的共同前置資料

以下兩條劇本都要靠「同一個可見範圍裡、建立者與所屬組織都不同」的資料才驗得出來,**先照這個順序建**:

1. **+user**(當前組織 = 南港店)在示範模組1 新增 **2 筆**(名稱好認一點,例如「南港-自建1 / 2」)。
2. **+tenant** 把當前組織切到**南港店**,新增 **1 筆**(「南港-他人」)。
3. **+tenant** 把當前組織切回**租戶頂層**,新增 **1 筆**(「頂層-他人」)。
4. 示範模組2 也照樣各建一筆(劇本 3 的對照組要用)。

**分類欄的先決條件**:示範模組1 的分類下拉來自欄位管理的「示範分類」,`fieldCategories` / `fields` 兩個端點都掛在 **`system.field-manager.view`** 底下。**「客服」角色沒有這個權限**,所以 +user 會看到:**列表沒有分類篩選、表單的分類欄退成唯讀並說明原因**(原值保留)。這本身就是劇本 2 / 12 要一起確認的畫面;要讓 +user 選得了分類,就在角色矩陣另外給他欄位管理模組與 `view` 權限。

---

## 逐條劇本

### 劇本 1 — wildcard 涵蓋未來(同層)

- **用哪一頁**:角色管理 → 編輯「客服」→「權限設定」分頁(權限矩陣)
- **帳號**:+tenant
- **E2E**:`apps/e2e/src/specs/scenario-01-wildcard.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)

1. 展開示範模組1 的節點,勾它的「全部(\*)」→ 同層的 view / create / edit / delete / 內部備註兩筆一起被連動勾起(M-03 / M-04)。
2. 往下看**編輯頁**節點:它的「全部(\*)」與 `show-history` **沒有**被勾起來。
3. 儲存、重新進入 → 勾選狀態一致。

**預期**:wildcard 只代表**該模組自己這一層**;子模組(隱藏頁)要各自勾自己的 `*`。資料庫只存一筆 `demo.sub.sample-one.*`(UI 看不到筆數,以 api 測試為準)。

**在畫面上驗不到的半條**:「下一版 seed 對示範模組1 新增權限 → 不改角色即擁有」要跨版本,由 api 測試覆蓋(`apps/api/src/demo-items-one/demo-items-one-permissions.test.ts`、`apps/api/src/permission/permission.test.ts`)。

### 劇本 2 — 資料範圍規則

- **用哪一頁**:「資料範圍」頁(建規則)+ 示範模組1 列表 / 詳情(看結果)
- **帳號**:規則用 **root**(資料範圍是根組織專屬模組,+tenant 的側欄沒有它 —— 見劇本 16);結果用 **+user**
- **前置**:上面「劇本 2 / 12 的共同前置資料」四筆
- **E2E**:`apps/e2e/src/specs/scenario-02-data-scope-rule.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)

1. **+user** 先看一次示範模組1 列表:看得到自己 2 筆 +「南港-他人」(同在南港店的可見範圍內);「頂層-他人」看不到(+user 不屬租戶頂層)。列表**沒有分類篩選**,表單的分類欄**唯讀並說明原因**(沒有 `system.field-manager.view`)。
2. **root** → 資料範圍 → 左清單選「示範項目」(`demo_items_one`)→ 新增一條規則:**套用對象 = 指定角色「客服」**、過濾條件 **建立者 屬於【操作者本人】** → 儲存。
3. **+user** 重新整理列表。
4. 點進被濾掉那筆的詳情網址(`/demo/sub/sample-one/view-page/<id>`)。
5. **root** → 回資料範圍頁把規則刪掉 → 儲存。

**預期**:步驟 3 列表只剩 **+user 自己建的 2 筆**,`totalCount` 一併變小;步驟 4 顯示查無資料(api 回 `NOT_FOUND` —— 列表與單筆同一個答案,不透露存在與否);步驟 5 之後恢復成步驟 1 的三筆。root 自己不受影響(規則的套用對象是「客服」角色,而且超級管理員的可見範圍是全部)。

### 劇本 3 — 未宣告對照

- **用哪一頁**:「資料範圍」頁左清單(root)+ 示範模組2 列表(+user)
- **帳號**:root + +user
- **E2E**:`apps/e2e/src/specs/scenario-03-undeclared-target.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)

1. **root** → 資料範圍頁 → 看左側的資料目標清單。
2. 維持劇本 2 的規則開啟狀態,**+user** 去看**示範模組2** 的列表。

**預期**:步驟 1 清單裡**沒有 `demo_items_two`** —— 示範模組2 的 seed 不宣告 `dataScopeTarget`。清單裡是「示範項目」(`demo_items_one`)與「購物清單」(`form_submissions`,表單模組的資料目標一模組一列);斷言看的是 `demo_items_two` 不在清單內,所以多一列不影響本劇本。步驟 2 示範模組2 的列表**不受規則影響**,+user 照樣看得到別人在南港店建的那筆:沒有規則命中的資料目標,只剩可見範圍保底。

### 劇本 4 — 頂層合成 OR / AND

- **用哪一頁**:「資料範圍」頁(規則合成切換)+ 示範模組1 列表
- **帳號**:root 設規則、+user 看結果
- **前置**:把「劇本 2 / 12 的共同前置資料」的**「南港-自建1」**改成狀態「已發布」(其餘三筆留「草稿」)。**一定要挑 +user 自己建的那一筆**:挑「南港-他人」或「頂層-他人」的話,規則①(建立者 = 本人)與規則②(狀態 = 已發布)的結果集不相交,OR 看起來就跟「無規則」沒兩樣,分不出合成到底有沒有生效
- **E2E**:`apps/e2e/src/specs/scenario-04-combine-op.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)

1. **root** → 資料範圍 → 示範項目 → 建**兩條都會命中 +user** 的規則:①套用對象 = 指定角色「客服」→ 建立者 屬於【操作者本人】;②套用對象 = 指定組織「南港店」→ 狀態 屬於【已發布】。
2. 頂層合成切 **OR** → 儲存 → **+user** 看列表。
3. 頂層合成切 **AND** → 儲存 → **+user** 看列表。

**預期**:OR = 兩條的**聯集**(自建的 + 任何已發布的);AND = **交集**(自己建的且已發布的)。兩種情況下租戶隔離都仍在最外層 —— 別的租戶的資料永遠不會因為合成方式而冒出來。

> 套用對象比對的是操作者的**啟用中角色**與**所屬組織的直接關聯**,不是可見範圍也不是管理範圍:所以②要寫「南港店」(+user 的所屬組織),寫「租戶A」不會命中他。

### 劇本 5 — 欄位級權限(綁父模組)

- **用哪一頁**:示範模組1 的**詳情頁**與**編輯頁**
- **帳號**:+tenant 改角色矩陣、+user 看畫面
- **E2E**:`apps/e2e/src/specs/scenario-05-field-permission.spec.ts`(手動觸發,見下方「E2E 怎麼跑」);末節「順手一起看」的 `[redacted]` 也在同一個 spec 裡

1. 「客服」角色**兩筆內部備註權限都不給** → +user 看任一筆的詳情頁與編輯頁。
2. +tenant 只加 `show-internal-note` → +user 重新登入(或重整)後再看兩頁。
3. +tenant 再加 `edit-internal-note` → +user 再看編輯頁。
4. (選配,需要開發者工具)在步驟 1 的狀態下直接對 `updateDemoItemOne` 送出帶 `internalNote` 的 input。

**預期**:

- 步驟 1:詳情頁**沒有「內部備註」那一列**、編輯頁**沒有那個欄位**(不是顯示空值 —— api 根本不把這個欄位放進回傳物件)。
- 步驟 2:兩頁都看得到內容,編輯頁的欄位是**唯讀**並附說明。
- 步驟 3:欄位變成可編輯,存得下去。
- 步驟 4:`FORBIDDEN` + `extensions.reason = "FIELD_FORBIDDEN"`(欄位**一出現在 input 裡就要權限**,送 `null` 清空也一樣)。這一條由 api 測試覆蓋,畫面上不必硬驗。

**順手一起看**:在步驟 1 的狀態下給 `edit-page.show-history`,打開編輯頁的變更歷程 —— 區塊看得到,但**畫面上看不到任何欄位值**:歷程每一列的樣板是 `{at} {actor} {action}`(`admin.json` 的 `entry`),只列「什麼時候、誰、做了什麼」。**內部備註被遮成 `[redacted]` 是 api 的行為**(`demoItemOneHistory` 回傳的 `before` / `after.internalNote`),要驗就看 api 回應(開發者工具的 Network,或 E2E 直接打 api),不會從側門外洩。「歷程要不要列出改了哪些欄位」尚未裁決。列表的關鍵字搜尋也**不比對**內部備註(help 文案有寫)。

### 劇本 6 — 頁面自有權限

- **用哪一頁**:示範模組1 的**新增頁**與**編輯頁**
- **帳號**:+tenant 改矩陣、+user 看畫面
- **E2E**:`apps/e2e/src/specs/scenario-06-page-permission.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)

1. 不給 `create-page.show-tips` → +user 進新增頁:**沒有填寫提示區塊**,但名稱 / 分類 / 備註照樣填得了、存得下去。
2. 給 `show-tips` → 重進新增頁:標題下方多出提示區塊。
3. 不給 `edit-page.show-history` → +user 進編輯頁:**沒有變更歷程區塊**,但編輯與儲存完全正常。
4. 給 `show-history` → 重進編輯頁:表單底部多出歷程區塊,列出這筆的建立 / 編輯紀錄(新到舊)。

**預期**:**區塊與功能兩件事互相獨立** —— 少一個區塊不影響這一頁本來的 CRUD。兩筆權限分別綁在**新增頁**與**編輯頁**自己的模組上(不是綁列表頁),所以只在那一頁生效。

### 劇本 7 — 路由防守

- **用哪一頁**:示範模組1 的列表頁 + 直接在網址列打三種網址
- **帳號**:+tenant 改矩陣、+user 試網址
- **E2E**:`apps/e2e/src/specs/scenario-07-route-guard.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)

1. 角色**不綁**新增頁模組(`demo.sub.sample-one.create-page`)→ +user 看列表:**沒有「新增」鈕**;手打 `/demo/sub/sample-one/create-page` → **無權限頁**。
2. 角色綁了新增頁模組、但**不給** `create` 權限 → 手打同一個網址:**頁進得去**,送出時被 api 擋(`FORBIDDEN`)。
3. 角色綁了詳情頁模組 → 手打 `/demo/sub/sample-one/view-page/<某筆 id>`:**進得去**,解出來的 id 就是要顯示的那一筆。
4. 手打 `/demo/sub/sample-one/whatever`(列表頁後面多接一段)→ **無權限頁**。

**預期**:**頁(模組)與功能(權限)是兩層,分開問**。步驟 3 / 4 的差別是「尾端動態參數的退路只對 hidden 模組成立」——詳情 / 編輯頁的模組路由本身不含識別碼,所以精準比對落空時**只對隱藏頁**再試一次「去掉最後一段」;link(列表頁)後面多一段仍然是打錯網址(ADR-0011「路由防守」、`apps/admin/src/lib/module-tree.ts` 的 `matchModuleRoute`)。

### 劇本 8 — 組織外

- **用哪一頁**:使用者管理(移除所屬組織)+ 角色管理「分配使用者」分頁 + 組織管理 + 示範模組1 列表
- **帳號**:+tenant 操作、+user 受影響
- **E2E**:`apps/e2e/src/specs/scenario-08-out-of-scope.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)
- **前置**:+user **同屬南港店與內湖店**,持一個**擁有組織 = 南港店**的角色(權限照抄「客服」),並把「客服」從他身上拿掉;+user 在南港店已建過資料
  - 為什麼不是「只屬南港店 + 客服」:只屬一個組織時移除會先撞 `LAST_ORG`(最後一個所屬組織不可移除);客服的擁有組織 = 租戶A,子樹包住內湖店,移掉南港店後客服仍有支撐,**永遠不會變組織外**
    (把 +user 放進南港店 / 內湖店可以走使用者管理的「選擇所屬組織」,也可以走組織管理 → 南港店 →「成員」分頁 →「加入成員」;兩者等價)

1. +tenant → 使用者管理 → 把 +user 自「南港店」移除,確認彈窗的 radio 選 **「保留所有角色授予」**。
   (**移除只有這一個入口** —— 組織管理的「成員」分頁刻意不做移除,因為移除要跑 dry-run 三檔。)
2. 看三處列表:使用者管理、角色管理的「分配使用者」、指派角色的彈窗;順手到組織管理 → 南港店 →「成員」分頁確認 +user 已不在成員清單裡。
3. +user 重新登入 → 進示範模組1 列表。

**預期**:步驟 2 三處都把 +user 標成**「組織外」**;步驟 3 他**照樣進得去**示範模組1(靠的就是那個已經組織外的南港店角色:角色授予還在、功能不受影響),但**看得到的資料收縮**(可見範圍少了南港店)。這正是「角色授予」與「所屬組織」兩件事分離的示範。

### 劇本 9 — 移除所屬組織的 dry-run 三檔

- **用哪一頁**:使用者管理 → 移除所屬組織的確認彈窗
- **帳號**:+tenant 操作;對象 = +user,先讓他也加入內湖店(**多所屬組織**)
- **E2E**:`apps/e2e/src/specs/scenario-09-org-removal-policy.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)
- **前置**:+tenant 在租戶A 底下建「東區」,把**南港店搬到東區底下**;另建兩個角色指派給 +user —— 「南港店長」(擁有組織 = 南港店)、「東區督導」(擁有組織 = 東區);「客服」(擁有組織 = 租戶A)照舊

1. 對象同時屬南港店與內湖店,並持有上面三個角色。
2. 把他自南港店移除 → 看彈窗列出的 dry-run 清單。
3. 分別選三個 radio 再確認,比對結果。

**預期**:清單要分得出 **(b)「只解除此組織擁有的角色」** 與 **(c)「解除所有因此失去資格的角色」** 兩種範圍;(a)「保留所有角色授予」則不解除任何授予。

| 角色     | 擁有組織 | 移除南港店後                         | (b)  | (c)  |
| -------- | -------- | ------------------------------------ | ---- | ---- |
| 南港店長 | 南港店   | 由被移除的組織擁有 + 失去子樹支撐    | 解除 | 解除 |
| 東區督導 | 東區     | 失去子樹支撐(東區底下只剩不屬的組織) | —    | 解除 |
| 客服     | 租戶A    | 內湖店仍在子樹內,不失去資格          | —    | —    |

**為什麼要「東區」**:只持「擁有組織 = 南港店」與「= 租戶A」兩個角色時,前者 (b)(c) 都列、後者 (b)(c) 都不列,兩檔的清單一模一樣。要分得出來,需要一個「**不是**被移除的組織擁有、卻**只靠它**支撐」的角色 —— 擁有組織在南港店的上層、而且不包住內湖店。單一組織的帳號同樣驗不到(ADR-0003「從組織移除使用者」)。

### 劇本 10 — 防越權

- **用哪一頁**:角色管理 → 權限矩陣
- **帳號**:+tenant(**不是** root —— 超級管理員直接全權放行,看不出限制)
- **E2E**:`apps/e2e/src/specs/scenario-10-out-of-reach.spec.ts`(手動觸發,見下方「E2E 怎麼跑」;硬送 `system.module-manager.*` 回 `ROLE_OUT_OF_REACH` 那一半直接打 api)

1. +tenant 新建或編輯一個角色 → 開權限矩陣。
2. 比對矩陣列出的模組 / 權限,與 +tenant 自己持有的(預設角色的內容)。
3. 打開租戶的「租戶管理員」(清單上掛「預設角色」標籤)看矩陣。

**預期**:矩陣**只列得出 +tenant 自己有的模組與權限**(顯示樹就是他的天花板);根組織專屬模組(模組與權限、資料範圍、租戶作業 `system.org-manager.tenant-ops`)根本不在樹上。預設角色是租戶的天花板,**只能縮不能擴**。

> 步驟 3 的「只能縮不能擴」(`shrinkOnly`)在這條劇本裡**與防越權分不出差別**:+tenant 自己的權限就是預設角色的內容,沒勾的列兩道鎖都會鎖、硬送也都是 `ROLE_OUT_OF_REACH`。兩道判準的順序(subset-only → 天花板 → `shrinkOnly`)由 api 測試 `apps/api/src/roles/role-kinds.test.ts` 覆蓋(`docs/modules/role-manager.md`「角色種類與可改動範圍」)。

### 劇本 11 — 儲存雙路(公開封面 / 私有附件)

- **用哪一頁**:示範模組1 的新增 / 編輯頁(上傳)+ 詳情頁(顯示與下載)
- **帳號**:+user
- **E2E**:`apps/e2e/src/specs/scenario-11-storage-two-paths.spec.ts`(手動觸發,見下方「E2E 怎麼跑」;檔案真的傳到 fake GCS 容器,本機沒有 Docker 時 skip)
  - 步驟 1–3 走畫面:新增頁選封面 PNG / 附件 PDF → 儲存(瀏覽器直傳)→ 詳情頁封面 `<img>` 載得出來、網址是公開 bucket 的不簽名 URL,不帶登入去讀拿得到同一個檔;按「下載」才出現連結,網址是私有 bucket 的 V4 簽名網址(`X-Goog-Expires` = 1h),讀得到同一個檔
  - 步驟 4 / 5 直接打 api:錯檔型 / 超過上限回 `UPLOAD_REJECTED`(訊息含 purpose);拿掉「客服」的 `view` 後同一個 token 要 `demoItemOneAttachmentUrl` 回 `FORBIDDEN`
  - **驗不到的**:fake GCS 不驗簽章、不分公開 / 私有,所以「簽名網址過一陣子就失效」仍屬 dev 環境的人工驗收

1. 新增一筆,封面上傳一張 PNG / JPG / WebP(2MB 內),附件上傳一份 PDF 或 ZIP(20MB 內)。
2. 進詳情頁:封面**直接顯示**;複製圖片網址到無痕視窗開 → 仍然看得到(公開穩定 URL,不過期)。
3. 附件那一列按「下載」→ 這時才向 api 要一條**短效簽名網址**;把那條網址留著過一陣子再開。
4. 上傳一個不在白名單的檔型(例如 `.exe`)或超過上限的檔。
5. +tenant 把「客服」的 `view` 權限拿掉 → +user 再按下載。

**預期**:步驟 2 公開 bucket、步驟 3 私有 bucket(網址會過期);步驟 4 回 `UPLOAD_REJECTED`(訊息含 purpose);步驟 5 api **拒發**簽名網址(`FORBIDDEN`)—— 私有檔案的每一次取用都重新檢查權限,不是上傳當下檢查一次就算數。

### 劇本 12 — 可見性開關

- **用哪一頁**:組織管理 → 編輯**租戶頂層**的「使用者可見自身組織的下層組織資料」開關;結果看示範模組1 列表與三個治理頁
- **帳號**:+tenant 切開關、看列表與三個治理頁;+user 看列表(南港倉庫那筆的收縮)
- **E2E**:`apps/e2e/src/specs/scenario-12-visibility-toggle.spec.ts`(手動觸發,見下方「E2E 怎麼跑」;切回 ON 走 api)
- **前置**(可見範圍 = **所有**所屬組織的聯集,ADR-0005 —— 照這個順序建,收縮才看得出來):
  1. 「劇本 2 / 12 的共同前置資料」四筆(+user 在南港店 2 筆、+tenant 在南港店 1 筆與租戶頂層 1 筆)。
  2. +tenant 在南港店底下建「南港倉庫」,暫時加入它、切到那裡新增 1 筆(「倉庫-他人」)。+user 只屬南港店,沒有這一層的話 ON / OFF 對他看不出差別。
  3. 把 +tenant 移出南港店與南港倉庫(彈窗選「保留所有角色授予」;他的角色擁有組織是租戶頂層,不受影響)。**不移的話 OFF 時他仍看得到南港店那三筆**,預期的「只剩租戶頂層那筆」不成立。
  4. **劇本 2 的資料範圍規則要先刪掉**,不然兩層收縮混在一起看不出是誰造成的。

1. +tenant → 組織管理 → 編輯租戶頂層 → 把開關**打開**(ON)→ 儲存(**新開通的租戶沒有設定 = `own`,開關是關的**,ADR-0005 的保守預設)。**+tenant** 看示範模組1 列表 → 5 筆全看得到;**+user** 看 → 南港店三筆 + 倉庫那筆,租戶頂層那筆看不到。
2. 把開關切成 **OFF**(`own`)→ 兩人各自重新整理列表。
3. 同一個狀態下,+tenant 去看**組織管理 / 使用者管理 / 角色管理**三個治理頁。
4. 順手確認 **+user** 的畫面:列表**沒有分類篩選**、表單分類欄**唯讀並說明原因**(分類選項掛在 `system.field-manager.view` 底下,「客服」沒有那個權限)。

**預期**:步驟 2 **業務資料收縮**:+tenant 只剩自己所屬組織(租戶頂層)那筆,+user 的「倉庫-他人」不見、南港店三筆還在;步驟 3 三個治理頁**完全不動**(樹根、使用者名單、角色候選都一樣)—— 治理模組看的是**管理範圍**,不看可見性開關(ADR-0005;細節見劇本 14)。既有的角色授予也不會因為切開關而變動。步驟 4 的兩個現象是「選項來源掛在別的模組權限底下」的正常結果,不是壞掉。

### 劇本 13 — 總覽也是模組

- **用哪一頁**:角色管理 → 權限矩陣;結果看登入後的落點與側欄
- **帳號**:+tenant 改矩陣、+user 登入
- **E2E**:`apps/e2e/src/specs/scenario-13-overview-module.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)

1. 確認「客服」角色**沒有**勾「總覽」模組(前置第 4 步只勾示範家族,本來就沒有)→ +user 登入。
2. 手打 `/overview`。
3. 在「客服」的矩陣勾上「總覽」→ +user 重新登入。(對客服這是**第一次給** `overview.*`,不是「勾回去」。)

**預期**:步驟 1 側欄**沒有「總覽」這一列**,登入後落在側欄深度優先第一個進得去的頁(「客服」只有示範家族,落點是示範模組1 `/demo/sub/sample-one`);步驟 2 顯示**無權限頁**(明確說是權限問題,不是壞掉);步驟 3 登入落回 `/overview`。總覽是正式模組(key `overview`),不是寫死的首頁。

### 劇本 14 — 管理範圍 vs 可見範圍

- **用哪一頁**:角色管理(建 / 換角色)+ 組織管理 / 使用者管理 / 角色管理(看樹根)+ 示範模組1 列表(看業務資料)
- **帳號**:+tenant 建角色與指派、被測的是 **+user**
- **E2E**:`apps/e2e/src/specs/scenario-14-management-scope.spec.ts`(手動觸發,見下方「E2E 怎麼跑」;六小節各一個 test,前置見 spec 開頭註解 —— 管理範圍算**全部**啟用中角色,所以看樹根的小節先把「客服」從 +user 身上拿掉)
  - 由角色決定:擁有組織 = 租戶頂層 → 樹根是租戶頂層、使用者清單列得到 +tenant;換成南港店 → 樹根只剩南港店、租戶頂層與內湖店不在樹上,api 的 `org(內湖店)` / `org(租戶頂層)` 回 `NOT_FOUND`
  - 不受可見性開關影響:ON → `OWN`(api 切)後三個治理頁、樹根、南港倉庫的搬移候選與 `orgTree` 完全相同,示範模組1 列表從 2 筆收縮成 1 筆(南港倉庫那筆不見)
  - 多根:+user 先加入內湖店(授予要過資格檢查,只屬南港店會拿到 `USER_NOT_ELIGIBLE`),再給南港店 + 內湖店兩個角色 → 兩個樹根;+tenant 停用內湖店那個 → 只剩南港店、`org(內湖店)` 回 `NOT_FOUND`
  - 無治理角色 = 空:解除、或授予還在但角色停用 → 三個治理頁手打網址都是無權限頁,`orgTree` / `org` 回 `FORBIDDEN`;「硬給 `view` 但樹是空的」在產品上造不出來(權限與管理範圍來自同一批角色),只由 api 的 `auth.test.ts`(沒有任何角色 → 管理範圍 `[]`)覆蓋到一半
  - 權限搬家:+tenant 在畫面上把自己租戶頂層的開關切 ON 並存檔、南港店的編輯彈窗沒有開關;api 對別的租戶頂層 `NOT_FOUND`、對南港店 `VALIDATION_FAILED`;只持 `view` 的 +user 沒有「編輯」、加上 `edit` 後彈窗裡也沒有開關,兩種狀態硬送都是 `FORBIDDEN`
  - 搬移候選:+tenant 編輯南港店 → 候選只有租戶頂層與內湖店(不含南港店自己與南港倉庫),租戶頂層的「上層組織」下拉停用;擁有組織 = 南港店的 +user 編輯南港倉庫 → 候選只剩南港店。api:搬去別的租戶 `NOT_FOUND`(root 做同一件事才是 `CROSS_TENANT`)、搬進自己子樹 `CYCLIC_MOVE`、+tenant 搬租戶頂層 `FORBIDDEN`

照上面第 14 條的六個小節逐一跑,每一節的落點:

| 小節                 | 怎麼做                                                                                                    | 看哪裡                                                                                                                                                    |
| -------------------- | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 由角色決定           | +user 只屬南港店、先拿掉「客服」;先給他一個擁有組織 = **租戶A** 的治理角色,再換成擁有組織 = **南港店** 的 | 組織管理頁的**樹根**:先是租戶頂層、換角色後變成南港店;其他分店整棵不回傳                                                                                  |
| 不受可見性開關影響   | 把租戶開關設成 `own`                                                                                      | 治理頁三處**完全不變**;示範模組1 的列表則收縮 —— 兩個範圍各管各的                                                                                         |
| 多根                 | +user 先加入內湖店,再同時給擁有組織 = 南港店與 = 內湖店的兩個角色                                         | 組織樹出現**兩個根**;停用其中一個角色 → 那棵子樹整個消失                                                                                                  |
| 無治理角色 = 空      | 把 +user 的治理角色全部解除或停用                                                                         | 治理頁進不去(`FORBIDDEN`);「硬給 `view` 但樹是空的」造不出來(見上)                                                                                        |
| 可見性開關的權限搬家 | 分別用 +tenant(含 `system.org-manager.*`)與 `view` + `edit`、沒有 `set-visibility` 的帳號去編輯組織       | 開關只在**自己租戶頂層**看得到、設得了;非租戶頂層送出 `VALIDATION_FAILED`,別的租戶 `NOT_FOUND`,沒有 `set-visibility` 的人彈窗裡沒有開關、硬送 `FORBIDDEN` |
| 搬移候選與頂層保護   | 在組織管理頁試搬移                                                                                        | 候選 = 管理範圍 ∩ 同租戶 − 自己子樹;搬進自己子樹 `CYCLIC_MOVE`、搬租戶頂層本身 `FORBIDDEN`                                                                |

### 劇本 15 — 側欄商標繼承

- **用哪一頁**:組織管理 → 編輯組織的商標欄;結果看側欄
- **帳號**:+tenant 設定、+user 看側欄
- **E2E**:`apps/e2e/src/specs/scenario-15-sidebar-logo.spec.ts`(手動觸發,見下方「E2E 怎麼跑」;商標真的傳到 fake GCS 容器,本機沒有 Docker 時 skip)
  - 三個步驟都走畫面:+tenant 在組織管理的編輯彈窗上傳 / 按「移除」清掉商標,+user(另一個 context)重新整理後看側欄的商標圖;「顯示的是哪一張」以讀回來的內容比對兩張顏色不同的測試圖。前置對照:沒人設商標時側欄是組織名稱的文字、沒有商標圖
  - 附帶驗「換圖即刪舊」:清掉南港店的商標後,步驟 2 那個物件已從 bucket 刪除(404)

1. 在**租戶頂層**上傳商標、南港店不設 → +user(屬南港店)重新登入看側欄。
2. 在**南港店**也上傳一張 → +user 再看。
3. 把南港店那張清掉 → +user 再看。

**預期**:步驟 1 顯示**租戶頂層**那張(沿 `ancestors` 由近到遠找第一個有商標的上層);步驟 2 改顯示自己的;步驟 3 又回到繼承來的那張。

### 劇本 16 — 租戶視角

- **用哪一頁**:組織管理 → 開通租戶(root)+ 租戶管理員登入後的整個側欄
- **帳號**:root 開通、+tenant 登入
- **E2E**:`apps/e2e/src/specs/scenario-16-tenant-perspective.spec.ts`(手動觸發,見下方「E2E 怎麼跑」;兩個 test)
  - 租戶視角:root 在畫面上開通(取消勾選示範群組)→ 首任管理員從啟用信設密碼(api)後登入:側欄有「系統管理 / 組織管理」、沒有「模組與權限」「資料範圍」「示範群組」;組織樹只有租戶頂層一個根、不掛「租戶」標籤,沒有「開通租戶」按鈕;欄位管理「性別」的種子選項開關停用、操作欄寫「由系統管理員維護」;root 重新整理組織管理 → 新租戶掛「租戶」。另以 api 對照副本的 `me.modules` 沒有 `demo` 家族
  - 撤銷開通:root 在畫面上對空租戶按「撤銷開通」→ 彈窗列出三樣、名稱沒打按不下去 → 打對送出 → 樹上消失,api 的 `org` / `user` / `role` 三者 `NOT_FOUND`、帳號登入 `INVALID_CREDENTIALS`,同一組名稱 / 帳號 / Email 以 api 重新開通成功;底下建了子組織的租戶送出後彈窗改列「還有下層組織」、不再給送出鈕,api 回 `PROVISION_NOT_REVOKABLE` 且 `reasons = [HAS_CHILDREN]`

1. **root** → 組織管理 → 開通租戶,模組勾選清單裡**取消勾選示範群組** → 建立。
2. 以新建的租戶管理員登入。
3. 回 root 視角看組織管理頁的樹。

**預期**:步驟 2 側欄**沒有「模組與權限」「資料範圍」**(兩者是根組織專屬模組)、**沒有「示範群組」**(開通時沒勾)、組織樹以**租戶頂層為根**、**沒有「開通租戶」按鈕**;欄位管理頁的全域選項是**唯讀**。步驟 3 root 看得到新租戶並標示「租戶」。

> 要跑劇本 1–15,開通時**要勾上**示範群組(或事後在該租戶的預設角色矩陣補勾),否則示範模組進不去。建議開兩個租戶:一個勾、一個不勾。

> **開錯可撤銷**:root 選到租戶頂層時動作列有「撤銷開通」,打對租戶名稱即把租戶、擁有者帳號、角色副本一起抹掉,同一組帳號可以重新開通;租戶底下一旦有其他資料就會被擋並列出原因。

### 劇本 17 — 擁有者保護

- **用哪一頁**:使用者管理(停用 / 移出租戶)+ 角色管理「分配使用者」(解除授予)+ 組織管理(對租戶頂層動手)
- **帳號**:+tenant 操作自己、root 覆核
- **E2E**:`apps/e2e/src/specs/scenario-17-owner-protection.spec.ts`(手動觸發,見下方「E2E 怎麼跑」;三個步驟各一個 test)
  - 步驟 1:畫面上自己那一列「停用」停用、「所屬組織」彈窗的租戶頂層鎖在勾選並寫明原因、角色管理「分配使用者」那一列掛「擁有者保護」且「移除」停用;api 三個動作都回 `OWNER_PROTECTED`,之後三樣都沒動
  - 步驟 2:+tenant 在畫面上選租戶頂層 → 停用 / 刪除停用、編輯彈窗的「上層組織」下拉停用;api 由 +tenant 與另拿到 `system.org-manager.*` 治理角色的 +user 各送一次停用 / 刪除 / 搬移,全是 `FORBIDDEN`;對照組 +tenant 停用 / 啟用南港店照常成功
  - 步驟 3:root 以 api 移出租戶頂層、解除預設角色、停用擁有者都成功,查回來 `enabled = false`、所屬組織只剩南港店、沒有副本角色
- **前置**:+tenant 要有**第二個所屬組織**(共同前置第 3 步已把他加進南港店)。`LAST_ORG`(最後一個所屬組織不可移除)的檢查在擁有者保護**之前**,只屬租戶頂層的話「移出租戶」先回 `LAST_ORG`,驗不到 `OWNER_PROTECTED`。

1. **+tenant** 試著停用自己的帳號 / 把自己移出租戶 / 解除自己的「租戶管理員」授予(角色清單上掛「預設角色」標籤的那個)。
2. **+tenant** 試著停用 / 刪除 / 搬移**租戶頂層**這個組織。
3. **root** 對步驟 1 的同一批動作再試一次。

**預期**:步驟 1 三個動作都回 **`OWNER_PROTECTED`**(租戶擁有者受保護);步驟 2 回 **`FORBIDDEN`**(租戶內任何人都動不了自己的租戶頂層);步驟 3 root **做得到**(根組織例外)。

### 劇本 18 — 表單生命週期(建立 → 發布 → 分派 → 填寫)

- **用哪一頁**:表單管理(root)+ 購物清單列表 / 新增 / 詳情(+user)
- **帳號**:root 設計與分派、+user 填寫
- **E2E**:`apps/e2e/src/specs/scenario-18-form-lifecycle.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)
- **前置**(走 api):客服角色的矩陣另勾「購物清單」與它的三個隱藏頁,給 `view` / `create` / `edit` / `delete`。

1. **root** → 表單管理 →「+ 建立表單」,填表單 key、名稱、所屬模組「購物清單」。
2. 在「設計」頁籤從元件面板加一個「單行文字」欄位,在屬性面板把顯示名稱改成要驗的名稱;沒選欄位時的「表單設定」把摘要的**標題**指到這一欄 →「存草稿」。
3. 「版本」頁籤 → 草稿那一列「發布」,填變更說明送出。
4. 右側標頭「分派租戶」→ 勾租戶A → 儲存。
5. **+user** → 購物清單 →「+ 新增」(只有這一張表單可填 → 直接進填寫頁)→ 填剛才的欄位 →「送出」。
6. 回列表看那一筆,再進詳情。

**預期**:步驟 3 後版本面板有「版本 1」、狀態已發布;步驟 5 送出後進詳情頁,看得到填的值、狀態「已完成」;步驟 6 列表的標題欄就是填的值(摘要槽),狀態「已完成」。沒有分派之前(步驟 4 前)+user 的新增鈕是停用的(沒有可填的表單)。

### 劇本 19 — 客製副本與退役目前版本

- **用哪一頁**:表單管理(+tenant)+ 購物清單新增(+user)
- **帳號**:+tenant 建客製表單與退役、+user 填寫
- **E2E**:`apps/e2e/src/specs/scenario-19-form-fork-retire.spec.ts`(手動觸發,見下方「E2E 怎麼跑」)
- **前置**(走 api):root 建一張共用表單、發布、分派給租戶A(劇本 18 的畫面流程在這裡不重跑);客服角色的矩陣同劇本 18;+tenant 是新開通的租戶管理員,預設角色已含「表單管理」的權限。

1. **+tenant** → 表單管理 → 選分派來的表單 →「以此為基底建新表單」,選基底版本、填表單 key 與名稱。
2. 在新表單的「設計」頁籤改欄位(例如加一個欄位)→「存草稿」→「版本」頁籤「發布」。
3. **+user** → 購物清單 →「+ 新增」:選單裡同時有共用表單與客製表單,選客製表單填寫送出。
4. **+tenant** → 客製表單的「版本」頁籤 →「退役目前版本」並確認。
5. **+user** 再按「+ 新增」。

**預期**:步驟 1 客製表單出現在清單、標「客製」,表單 key 之後不能改;步驟 3 客製表單看得到步驟 2 加的欄位;步驟 5 選單裡**沒有**客製表單(只剩共用表單),直接打它的新增網址顯示「這張表單目前不能新增」;步驟 3 送出的那一筆在列表與詳情照常看得到。另以 api 斷言:以退役的表單 `createFormDraft` 回 `FORBIDDEN`(`FORM_NOT_AVAILABLE`)。

### 劇本 20–24 的共同前置(走 api)

- root 建一張共用表單(標題 + 天數或金額,摘要標題 = 標題)掛在「請假」模組、發布、分派給租戶A。
- +tenant 建:「南港廚房」(南港店的下層)與申請人(所屬 = 南港廚房)、南港店的**主管**(組織管理的「主管」欄)、人資(持「人資」角色)、副理;「申請人」角色給請假與申請中心,「審核員」角色給申請中心,都指派好。
- +tenant(租戶管理員,預設角色含流程管理與阻擋清單的 `system.workflows.blocked-page.reassign`)以 api 建客製流程、發布、在表單管理把表單綁上它;申請人以 api 送出。

### 劇本 20 — 主管 + 人資兩級核准(含主管解析)

- **用哪一頁**:申請中心(待我審核 → 詳情頁的審核區塊;我的申請)
- **帳號**:主管、人資、申請人
- **E2E**:`apps/e2e/src/specs/scenario-20-workflow-two-level.spec.ts`
- **前置**:共同前置;流程「直屬主管(主管第 1 層)→ 人資(角色 = 人資)」。

1. 申請人送出後,**主管** → 申請中心 →「待我審核」→ 那張單「審核」→ 詳情頁的審核區塊「核准」。
2. **人資** → 同樣路徑 →「核准」。
3. **申請人** → 申請中心「我的申請」。

**預期**:送出後任務派給**南港店**的主管(申請人所屬的南港廚房沒有主管,往上一層找到),人資還沒有任務;步驟 1 後關卡進度「直屬主管:已通過」「人資:審核中」;步驟 2 後「人資:已通過」;步驟 3 那張單「已完成」。

### 劇本 21 — 退回修改 → 再送 → 駁回

- **用哪一頁**:申請中心詳情(主管)、我的申請 → 請假編輯頁(申請人)
- **帳號**:主管、申請人
- **E2E**:`apps/e2e/src/specs/scenario-21-workflow-return-reject.spec.ts`
- **前置**:共同前置;流程只有「直屬主管」一關。

1. **主管** → 待我審核 → 那張單 →「退回修改」,填理由送出。
2. **申請人** → 我的申請 → 那張單「繼續編輯」→ 改標題 →「送出」。
3. **主管** → 待我審核 → 新標題的那張 →「駁回」,填理由送出。
4. **申請人** → 我的申請。

**預期**:步驟 1 後狀態「已退回」;步驟 2 編輯頁提示「被退回修改,改好再送出會重新審核」,送出後修訂 +1、換一個新的審核實例、從第一關重審;步驟 3 主管看到的是新標題;步驟 4 狀態「已駁回」、沒有「繼續編輯」。駁回 / 退回不填理由送不出去。

### 劇本 22 — 審核者停用 → 阻擋 → 改派 → 完成

- **用哪一頁**:流程管理 → 阻擋清單(+tenant)
- **帳號**:+tenant、副理
- **E2E**:`apps/e2e/src/specs/scenario-22-workflow-blocked-reassign.spec.ts`
- **前置**:共同前置;流程只有一關「主管審核」(指定使用者 = 南港店主管);申請人送出後,+tenant 在使用者管理停用那位主管。

1. **+tenant** → 流程管理 →「阻擋清單」→「阻擋」頁籤找到那張單 →「改派」→ 選副理 →「確定」。
2. **副理** 核准。

**預期**:停用後那張單「審核中(待處理)」(提交 `blocked`);步驟 1 後阻擋清單變空、實例回到審核中、計畫項目換成副理(原承辦人記在改派紀錄);步驟 2 後「已完成」。不會自動改派、不會自動跳過。

### 劇本 23 — 核准後作廢 → 複製為新單 → 重審

- **用哪一頁**:請假列表、請假詳情(審核區塊)、請假編輯頁
- **帳號**:申請人(主管以 api 核准)
- **E2E**:`apps/e2e/src/specs/scenario-23-workflow-void-copy.spec.ts`
- **前置**:共同前置;流程只有「直屬主管」一關;申請人送出、主管核准。

1. **申請人** → 請假列表:那張「已完成」的單。
2. 列上「作廢」→ 填作廢理由 → 作廢。
3. 進詳情 → 審核區塊「複製為新單」→ 進編輯頁 →「送出」。

**預期**:步驟 1 綁流程的已完成單**沒有「編輯」、只有「作廢」**(不需要主管同意);步驟 2 後狀態「已作廢」;步驟 3 新單記著來源、送出後重新走流程(主管收到新任務),原單維持「已作廢」並記著新單(只能複製一次)。

### 劇本 24 — 採購單三部門平行(一條阻擋改派、全部通過後匯合核定)

- **用哪一頁**:阻擋清單(+tenant)、申請中心詳情(原部門主任)
- **帳號**:+tenant、原部門主任(其餘審核者以 api 決定)
- **E2E**:`apps/e2e/src/specs/scenario-24-workflow-parallel.spec.ts`
- **前置**:共同前置(表單名「採購單」);流程「原部門初審 → 財務部審核(兩人,任一人)/ 法務部審核(兩人,全部)/ 採購部審核 → 三部門匯合 → 原部門確認」(初審與確認是同一人、不同關卡);申請人送出、初審核准;+tenant 停用採購承辦人;財務一人、法務兩人核准。

1. **+tenant** → 阻擋清單 → 那張單的採購任務「改派」給另一位採購 →「確定」;新採購承辦人核准。
2. **原部門主任** → 待我審核 → 那張單 → 看關卡進度 →「核准」。

**預期**:初審後三條分支同時派任;採購那條阻擋時,財務與法務照審,匯合等採購(實例進行中的只剩採購);步驟 1 後三條都通過、匯合完成,只剩「原部門確認」;步驟 2 詳情的關卡進度逐列顯示「財務部審核:已通過」「法務部審核:已通過」「採購部審核:已通過」「三部門匯合:已匯合」「原部門確認:審核中」,核准後全案「已完成」。

---

## 一頁看完:劇本 → 頁面 → 帳號

「E2E」欄 = `apps/e2e/src/specs/` 底下對應的 spec 檔名,「尚未」= 還是人工驗收。

| 劇本                    | 主要頁面                                  | 操作帳號 / 被測帳號  | E2E                                             |
| ----------------------- | ----------------------------------------- | -------------------- | ----------------------------------------------- |
| 1 wildcard              | 角色管理 → 權限矩陣                       | +tenant              | `scenario-01-wildcard.spec.ts`                  |
| 2 資料範圍規則          | 資料範圍頁 + 示範模組1 列表 / 詳情        | root / +user         | `scenario-02-data-scope-rule.spec.ts`           |
| 3 未宣告對照            | 資料範圍頁左清單 + 示範模組2 列表         | root / +user         | `scenario-03-undeclared-target.spec.ts`         |
| 4 頂層合成 OR / AND     | 資料範圍頁 + 示範模組1 列表               | root / +user         | `scenario-04-combine-op.spec.ts`                |
| 5 欄位級權限            | 示範模組1 詳情頁 + 編輯頁                 | +tenant / +user      | `scenario-05-field-permission.spec.ts`          |
| 6 頁面自有權限          | 示範模組1 新增頁 + 編輯頁                 | +tenant / +user      | `scenario-06-page-permission.spec.ts`           |
| 7 路由防守              | 示範模組1 列表 + 直接打網址               | +tenant / +user      | `scenario-07-route-guard.spec.ts`               |
| 8 組織外                | 使用者管理 / 角色管理 / 示範模組1 列表    | +tenant / +user      | `scenario-08-out-of-scope.spec.ts`              |
| 9 移除三檔              | 使用者管理 → 移除所屬組織彈窗             | +tenant / 多組織帳號 | `scenario-09-org-removal-policy.spec.ts`        |
| 10 防越權               | 角色管理 → 權限矩陣                       | +tenant              | `scenario-10-out-of-reach.spec.ts`              |
| 11 儲存雙路             | 示範模組1 表單 + 詳情頁                   | +user                | `scenario-11-storage-two-paths.spec.ts`         |
| 12 可見性開關           | 組織管理(開關)+ 示範模組1 列表 + 三治理頁 | +tenant / +user      | `scenario-12-visibility-toggle.spec.ts`         |
| 13 總覽也是模組         | 角色管理 → 權限矩陣 + 登入落點            | +tenant / +user      | `scenario-13-overview-module.spec.ts`           |
| 14 管理範圍 vs 可見範圍 | 角色管理 + 三治理頁 + 示範模組1 列表      | +tenant / +user      | `scenario-14-management-scope.spec.ts`          |
| 15 側欄商標繼承         | 組織管理 → 商標 + 側欄                    | +tenant / +user      | `scenario-15-sidebar-logo.spec.ts`              |
| 16 租戶視角             | 組織管理 → 開通租戶 + 租戶側欄            | root / +tenant       | `scenario-16-tenant-perspective.spec.ts`        |
| 17 擁有者保護           | 使用者管理 / 角色管理 / 組織管理          | +tenant / root       | `scenario-17-owner-protection.spec.ts`          |
| 18 表單生命週期         | 表單管理 + 購物清單列表 / 新增 / 詳情     | root / +user         | `scenario-18-form-lifecycle.spec.ts`            |
| 19 客製副本與退役       | 表單管理 + 購物清單新增                   | +tenant / +user      | `scenario-19-form-fork-retire.spec.ts`          |
| 20 兩級核准與主管解析   | 申請中心(待我審核 / 詳情 / 我的申請)      | 主管 / 人資 / 申請人 | `scenario-20-workflow-two-level.spec.ts`        |
| 21 退回 → 再送 → 駁回   | 申請中心詳情 + 請假編輯頁                 | 主管 / 申請人        | `scenario-21-workflow-return-reject.spec.ts`    |
| 22 停用 → 阻擋 → 改派   | 流程管理 → 阻擋清單                       | +tenant / 副理       | `scenario-22-workflow-blocked-reassign.spec.ts` |
| 23 作廢 → 複製為新單    | 請假列表 / 詳情 / 編輯頁                  | 申請人               | `scenario-23-workflow-void-copy.spec.ts`        |
| 24 三部門平行           | 阻擋清單 + 申請中心詳情                   | +tenant / 原部門主任 | `scenario-24-workflow-parallel.spec.ts`         |

**唯一在畫面上驗不到的**:劇本 1 的「下一版 seed 新增權限即擁有」(跨版本),由 api 測試覆蓋。

## E2E 怎麼跑

有 spec 的劇本**不必再人工走一次**,但 E2E **只在手動觸發時跑**(不進每個 PR 的 CI,理由見 TEST-05):

```bash
pnpm e2e                       # 本機全部(harness 會自己 build、起 Mongo / api / admin)
E2E_GREP="劇本 7" pnpm e2e     # 只跑某一條
gh workflow run e2e.yml --ref <分支> -f grep="劇本 7"   # CI(.github/workflows/e2e.yml)
```

Windows 上用 PowerShell 設變數:`$env:E2E_GREP="劇本 7"; pnpm e2e`(Bash 的 `E2E_GREP=… pnpm e2e` 前綴會被 agent 的 worktree 守衛擋下,見 `apps/e2e/README.md`)。

harness、前置資料與寫法見 `apps/e2e/README.md` 與 `docs/standards/testing/testing.md` 的 TEST-11。
**「E2E」欄是「尚未」的劇本仍照本文件人工驗收**;補上 spec 的票要同時把這一欄改掉。

**額外一條(不在 24 條內)**:模組與權限頁 —— root 停用「示範模組2」→ +tenant 的側欄少一項;重新啟用只開這一個節點(停用父模組時整棵子樹視同停用)。
