# 審核流程(現況說明)

回答「表單送出之後怎麼審」:流程誰建、怎麼綁到表單、送出時發生什麼、關卡怎麼找人、決定怎麼算、卡住了怎麼辦、誰看得到什麼。建立在表單引擎(`form-engine.md`)之上 —— 沒綁流程的提交「送出即完成」,綁了流程的表單則在送出後進審核,核准才完成。api 的落地細節(送出寫入順序、推進執行器、決定的原子寫入、錯誤碼、稽核)在 `docs/modules/workflows.md`;純邏輯在 `@repo/domain/workflow`;資料表見 `docs/data-model.md`。

## 流程、版本、分派、綁定

| 部分                        | 誰建 / 誰管            | 在哪                                                          |
| --------------------------- | ---------------------- | ------------------------------------------------------------- |
| 固定模組:申請中心、流程管理 | 開發者(seed)           | `apps/db-migrator/seeds/modules/apply-center.ts`、`system.ts` |
| 流程:關卡、版本             | 平台(共用)/ 租戶(客製) | 「流程管理」頁(`system.workflows`)                            |
| 分派(共用流程 → 租戶)       | 平台                   | 業務關聯 `org_workflow`                                       |
| 綁定(租戶的表單 → 流程)     | 租戶管理員             | 業務關聯 `org_form_workflow`,在「表單管理」設                 |
| 主管(組織 → 使用者)         | 租戶管理員             | 核心關聯 `org_manager`,在「組織管理」設                       |
| 實例、任務(執行中的審核)    | 系統                   | `workflow_instances`、`workflow_tasks`                        |

- 流程和表單一樣是**執行期資料**,不寫進 seed;seed 只多兩個固定模組與一個表單模組範例「請假」。
- **共用流程**(`ownerOrgId = null`)不能指定使用者、角色關卡只存佔位(例如「人資」);含角色佔位的共用流程租戶不能直接綁,要**以它為基底建客製流程**,把佔位指到自己的角色。租戶也可以直接建客製流程。
- 版本生命週期與表單版本**同一份實作**(`apps/api/src/versioning/version-lifecycle.ts`):草稿 → 發布中 → 已發布 → 已退役,四步發布、中斷可冪等重試;流程少了欄位級權限那一步。
- **一張表單最多綁一個流程,多張表單可以綁同一個**(唯一鍵沿用業務關聯的 `(tenantId, type, firstId, secondId)`)。沒有綁定 = 不走流程,維持送出即完成。綁定時先做「能不能直接綁」的檢查(共用流程的角色 / 指定使用者、表單欄位來源對不上),只是設計輔助,不取代送出時檢查。

正本:`apps/api/src/workflows/workflow-design/`、`packages/domain/src/workflow/validate-definition.ts`

## 流程結構:直線與分流 / 匯合

- **直線**:版本沒有 `edges`,`steps[]` 的順序就是流程。
- **分流 / 匯合**:一個審核關卡完成後同時啟動多條分支,各分支(一關或多關)最後接到同一個**匯合節點**(`kind: "join"`,系統節點,不派人、不能被跳過);匯合等**全部**分支通過或依條件跳過才完成,之後可結束或接審核關卡。
- 允許的形狀:可以有多組分流,但不巢狀、不交叉、不迴圈;審核關卡入線 ≤ 1;每關在同一實例內最多執行一次。條件分支、迴圈、「N 條中 M 條通過」都不做。
- **會簽模式**(關卡內幾個人要過:`any` / `all`)和匯合(跨分支、固定全部)是兩件事。
- 定義檢查器 `validateWorkflowDefinition` 前後端同一份:設計器即時跑,發布前 api 再跑,每個錯誤定位到關卡。

設計器不讓使用者自由拉線:admin 以「段落串」(主線關卡 / 一組分流)編輯,再產生 `steps` / `edges`,形狀本身就守住上面的規則;節點位置由 dagre 自動直式排版。

正本:`packages/domain/src/workflow/graph.ts`、`validate-structure.ts`、`apps/admin/src/lib/workflow/flow-model.ts`

## 送出:從提交到實例

每次送出(含退回後再送出)都先做**送出時檢查**:這筆提交綁的表單版本 + 現在綁的流程版本搭不搭。四種會擋:流程引用的欄位在這個表單版本不存在或不合型(跳過條件、表單欄位來源)、綁的流程沒有發布版、進過審核的單綁定被解除或流程被收回;另外綁定指向被收回或不存在的流程,一律擋 —— 不會默默免審。

通過後依序:建實例(`linking`,還不派單)→ 提交同一次更新成「審核中」並連上實例 → 實例改 `running`、從起點開始 → 推進。任一步中斷,重送同一次送出會從缺的那步接下去,不會多一個實例、也不會多一個修訂號。**送審不經過「已完成」**。

實例記 `(workflowKey, workflowVersion)` 與該修訂的摘要快照;之後流程改版、退役、收回分派、表單停用都**不影響進行中的實例**,只影響新送出。

正本:`apps/api/src/workflows/workflow-engine/workflow-submit.service.ts`、`packages/domain/src/workflow/submit-check.ts`

## 實例是唯一權威

| 放在實例上(權威)                                                     | 投影 / 衍生                              |
| -------------------------------------------------------------------- | ---------------------------------------- |
| 進行中的關卡(平行時多個)、每關狀態、派任計畫、被接受的決定、終局結果 | 審核任務(`workflow_tasks`)的狀態與承辦人 |
| 歷程(時間軸)                                                         | 提交的狀態與「待處理」標記               |

- **推進**(`advance`)是引擎唯一會改關卡與任務的動作:讀實例與全部任務 → 依判斷表找出該做的事(建任務、關卡完成、啟動後繼、匯合、終局、收尾、解除阻擋)→ 每個寫入都是條件更新 → 重讀再判斷,直到沒事可做。任何入口重跑結果相同。判斷表是純函式(`advance.ts`),不碰資料庫。
- **審核者只在進關時解析一次**,寫進派任計畫後不重算:角色後來多了人不補任務;角色被拿掉的人仍可審;人被停用或移出租戶走「阻擋」。
- **主管**:從**提交所屬組織**往上找第一組主管(第 2 層再往上一組),上界是提交的租戶;申請人自己剔除、剔除後為空就再往上。
- **決定**先原子地記進實例(條件:關卡仍進行中、我仍是這一項的有效承辦人、還沒決定過)才算被接受;兩人同時決定都會被記下,誰算數由推進依接受順序判,晚到的記成「晚於結果」。關卡已結束時回「此關已結束」,不是錯誤。
- **終局**:任一關卡(含任一分支)有效駁回 / 退回 → 全案終局,其他分支沒決定的任務取消、已做的決定保留;全部走完且終點完成 → 核准,提交「已完成」並**鎖定**(只能作廢)。

正本:`packages/domain/src/workflow/advance.ts`、`evaluate.ts`、`projection.ts`、`apps/api/src/workflows/workflow-engine/`

## 提交的七個狀態

| 狀態   | 意思                                        | 申請人能做                    |
| ------ | ------------------------------------------- | ----------------------------- |
| 草稿   | 存了沒送                                    | 改、送、刪                    |
| 審核中 | 有進行中的實例(被阻擋時標「待處理」)        | 還沒人審過前可撤回            |
| 已退回 | 被退回修改                                  | 改完再送(修訂 +1、重新審)、刪 |
| 已撤回 | 申請人自己收回                              | 同已退回                      |
| 已完成 | 沒走流程:送出即此,可再改;走過流程:核准,鎖定 | 走過流程的只能作廢            |
| 已駁回 | 審核駁回,不能再改、不能再送                 | 另開一張                      |
| 已作廢 | 核准後作廢(不需審核),內容凍結               | 複製為新單                    |

正本:`packages/domain/src/workflow/types.ts`(`SUBMISSION_STATUSES`)、`docs/modules/workflows.md`「狀態機」

## 卡住了:阻擋、改派、重試推進

- **阻擋**:關卡解析不到有效審核者,或承辦人失效(停用 / 移出租戶),就停住等人處理 —— **不自動改派、不自動跳過**。其他分支照常審,匯合等它解除。
- 流程管理者在「流程管理 → 阻擋清單」處理(權限 `system.workflows.blocked-page.reassign`):**改派**(換承辦人,原承辦人記為曾持有)、**新增審核者**(只給解析為空的關卡)、**重試推進**。
- 「需要推進」篩選列的是中斷過、還有事可做的實例(判斷表 dry-run 的結果);候選太多時只檢查最久沒動的一批並提示。

正本:`apps/api/src/workflows/workflow-engine/blocked-instances.service.ts`、`assignee-invalidation.service.ts`

## 申請中心與誰看得到什麼

- **申請中心**(`apply-center`)是固定模組,兩個**跨模組**頁籤:「我的申請」(我送的、走過流程或表單綁了流程的)與「待我審核」(派給我的任務);右上「新申請」選模組 → 選表單 → 進該模組的新增頁。表單都留在各自的業務模組。
- 申請中心有自己的隱藏詳情頁(`/apply-center/view-page/<實例 id>`,通知信的連結也是它),**不經業務模組的頁面權限**:沒有該模組權限的審核者也看得到他要審的那一張。
- 讀取授權統一走 `canReadSubmissionRevision`:原本的讀取規則(模組 view + 可見範圍 + 資料範圍)照舊;**申請人**讀得到自己的單(全部修訂);**任務持有者**(現在或曾經)**只讀得到他審的那個修訂**的快照與那個實例的歷程,讀不到之後的草稿與其他修訂;流程管理者在阻擋清單只看得到標題,不看內容。
- 任務清單、通知信、詳情的摘要一律讀**實例上的快照**,不讀提交最新的摘要 —— 否則只審過舊修訂的人會看到新修訂的標題。

正本:`apps/api/src/workflows/apply-center/`、`workflow-engine/submission-read-access.service.ts`

## 前端怎麼組

- 審核區塊(`apps/admin/src/components/workflow/ApprovalSection/`)掛在表單詳情下方(預設組裝含,客製頁自己放),申請中心詳情頁也用它:關卡 / 分支進度、我的任務動作、申請人的撤回 / 作廢 / 複製為新單、時間軸、重試推進。
- 流程設計器、阻擋清單、申請中心、表單管理的流程綁定欄的頁面細節見 `docs/modules/workflows.md`「admin 頁面」。

## 不用交易

和整個底座一樣不用 Mongo 交易(單節點沒有 transaction,ADR-0007、ADR-0009):每個寫入是單文件的條件更新(實例用 `editVersion` CAS、任務用 `(instanceId, taskKey)` 唯一鍵),每個中斷都有冪等的恢復入口(重送同一次送出、重試推進)。通知信是盡力而為:寄信失敗不影響推進,「寄過沒」看實例歷程。

正本:`docs/modules/workflows.md`「推進」「通知信」、`docs/adr/0007-base-fields-and-data-protection.md`、`docs/adr/0009-tenant-provisioning.md`
