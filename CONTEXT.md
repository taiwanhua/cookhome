# CookHome

家常食譜平台(front 對外、admin 後台、api),底層是一套可複製到其他專案的多租戶後台底座。本檔是詞彙表 — 只定義語言,不含實作細節。

## Language

### 帳號與租戶

正本:`docs/concepts/accounts-and-tenants.md`、`apps/api/src/auth/`、`apps/api/src/orgs/`

**底座(Platform Base)**:
可複製到其他專案的共用後台框架:組織/使用者/角色/模組/權限/欄位管理/認證。與各專案的業務域(如食譜)相對。
_Avoid_: 框架、共用系統

**組織(Org)**:
租戶單位,可形成樹(有父組織)。所有租戶資料的隔離邊界。
_Avoid_: 公司、團隊、tenant

**租戶(Tenant)**:
根組織的直接子組織 — 平台上一個獨立的經營主體,其下可再分部門/分店。根組織本身是平台運營者。

**租戶短碼(slug)**:
租戶頂層組織的短代號(`orgs.slug`,小寫英文開頭、只用小寫 / 數字 / 底線,2–20 字),開通時填、全站唯一、只有根組織能改。客製表單 key 的預設後綴。
_Avoid_: 租戶代碼、org code

**系統內建(isSystem)**:
種子記錄上的保護旗標:不可刪除、不可改 key,僅 seed 腳本可維護(如 super-admin 角色、核心模組)。

**使用者(User)**:
登入 admin 後台的帳號,可屬於多個組織,但同一時刻以單一「當前組織」操作。與會員是兩個獨立的帳號體系,憑證與 token 互不通用。
_Avoid_: 用戶、管理員帳號、admin user

**超級管理員(Super Admin)**:
系統內建的種子角色,僅可授予根組織的使用者。持有者在權限解析時**直接全權放行**,不靠任何權限記錄(資料庫裡沒有「全域萬用字元」這種權限)。不是使用者上的欄位。

**租戶管理員**:
種子角色**模板**(key `tenant-admin`,擁有組織=根組織,永不直接授予);開通租戶時複製一份到該租戶名下(各租戶各持一份、可自行客製、互不影響)。模板 = 「租戶可拿到的最大集合 + 預設組合」:權限為全部模組 wildcard 扣除根組織專屬模組;開通時由根組織勾選實際開放的模組(預設全勾)。副本授予首任租戶管理員,之後租戶可再授予他人;副本即該租戶的天花板(防越權:只能下放自己擁有的)。
_Avoid_: 租戶管理者

**租戶擁有者**:
`orgs.ownerUserId` 指向的使用者(僅租戶頂層有值);開通時 = 首任租戶管理員。受保護:其「租戶管理員」授予不可被解除、不可被移出租戶、不可被停用(根組織例外);v1 轉移僅根組織可操作。與「持有租戶管理員角色的人」不同 — 後者可以有多個。
_Avoid_: 首任管理員(僅描述來源時用「首任租戶管理員」)

**預設角色**:
「租戶管理員副本」在 **UI 與 help 文案中的稱呼**(角色清單上的標籤、`Role.isTemplateCopy`)。租戶使用者看得到的畫面不出現「租戶」「開通」這類平台視角詞彙(FIGMA-04),所以那句話寫成「系統開通時幫你準備好的角色」並標示「預設角色」。技術文件、模型與 PR 一律用「租戶副本」。
_Avoid_: 租戶副本(UI 文案中)、範本角色、模板角色

**系統管理員**:
UI 文案中對「平台營運方(根組織層級)」的統一稱呼,用於「請聯絡系統管理員」與「僅系統管理員可見/可用」類語句(內部實際是開發流程或根組織管理者處理,使用者不需要知道)。僅作為文案用語,不是角色或欄位。
_Avoid_: 系統開發團隊、開發管理員、開發人員、平台管理者、平台管理員(UI 文案不出現)

**會員(Customer)**:
登入 front 的帳號(collection 名維持 Customer)。與使用者分表、分模組,依業務邏輯與組織關聯。
_Avoid_: 顧客、用戶、消費者

### 授權

正本:`docs/concepts/authorization.md`、`docs/concepts/data-layer-and-isolation.md`、`apps/api/src/permission/`、`apps/api/src/database/operator-context.ts`

**角色(Role)**:
權限的集合,由單一組織擁有並自管(同一人在不同租戶可有不同角色)。僅可授予「所屬組織落在擁有組織子樹內」的使用者。會員不進角色體系。

**所屬組織**:
**使用者**與組織的隸屬關係(`org_user`)。決定他出現在哪些名單、資料可見範圍多大。(角色側用「擁有組織」,兩詞不互換)
_Avoid_: 成員資格、隸屬關係、使用者的組織

**擁有組織**:
**角色**屬於哪個組織(`org_role`);每個角色只屬一個組織。它同時是這個角色的**管轄邊界**:持有者在治理模組能操作的組織 = 擁有組織的整棵子樹(見「管理範圍」)。(使用者側用「所屬組織」,兩詞不互換)。例外:UI 與 help 的白話文案可寫「此角色所屬的組織」,模型與技術文件一律用「擁有組織」。
_Avoid_: 角色的組織、所有者組織(技術文件中)

**當前組織**:
使用者此刻在 AppBar 組織切換器選中的那一個所屬組織,存在 token 裡。只決定兩件事:新資料寫入哪個組織(orgId)、業務模組的預設篩選。**不參與權限計算,也不影響可見範圍**。
_Avoid_: 語境、組織語境、當前租戶

**操作者**:
此刻登入、正在執行這個動作的使用者;與被動作的對象相對(例:小華把角色授予小明 — 小華是操作者,小明是對象)。防越權與可見範圍檢查都作用在操作者身上。
_Avoid_: 管理員(語意過寬)、當前使用者

**可見範圍**:
操作者所有所屬組織的聯集(租戶可見性開關 ON 時各自含其下層);**只管業務資料看多少**,資料範圍規則只能在其中再縮小。根組織的可見範圍是全部(程式以 `"all"` 表示,不列舉 id)。治理模組不看它,看「管理範圍」。
_Avoid_: 可見組織集合、資料權限範圍

**管理範圍**:
操作者在**治理模組**(組織管理、使用者管理、角色管理)能操作的組織集合 = 其持有的**啟用中角色**之**擁有組織子樹**的聯集。由角色決定、與持有者所屬哪裡無關、不受可見性開關影響;持有超級管理員 = 全部;沒有任何治理角色 = 空。組織樹的根就是各擁有組織;搬移候選 = 管理範圍 − 自己這棵子樹。程式以 `managedOrgIds` 表示。
_Avoid_: 管轄範圍、治理範圍、可操作組織

**操作者上下文**:
傳給資料層(BaseRepository)每個方法的第一個參數,打包「這個人是誰、站在哪裡、看得到 / 管得到哪些」:操作者 id、當前組織、可見範圍、管理範圍,以及資料範圍規則要用的**所屬組織(直接關聯)**與**啟用中角色**。四個組織 / 角色集合各有各的用途、不可互相代用(`docs/concepts/data-layer-and-isolation.md`「操作者上下文的四個集合」)。沒有它的查詢會被直接拒絕。
_Avoid_: context、scope、請求上下文

**租戶過濾**:
資料層自動在每個查詢加上「orgId ∈ 操作者可見範圍」這個動作;個別功能不自己寫、也繞不過。沒帶操作者上下文就拋錯,不會靜默回傳全部。
_Avoid_: 中央過濾、自動過濾、租戶隔離過濾

**治理模組**:
管「平台結構本身」的模組:組織、使用者、角色、模組與權限、欄位管理。隨底座出貨,每個專案同一套;頁面一律攤開整個管理範圍(工作天生跨組織;`docs/concepts/data-layer-and-isolation.md`「管理範圍 vs 可見範圍」)。
_Avoid_: 底座模組(可用但以治理模組為準)、系統模組

**業務模組**:
管租戶實際生意的模組(如食譜、廣告),每個專案不同。頁面慣例上預設跟當前組織篩選(個別模組需求不同可自行決定);新資料寫入當前組織。
_Avoid_: 功能模組、應用模組

**角色授予**:
把角色授予使用者(`user_role`)。UI 名稱:使用者側動作叫「指派角色」、角色側分頁叫「分配使用者」— 同一件事的兩個入口。授予不改變所屬組織,也不改變資料可見範圍。
_Avoid_: 分配角色、綁定角色

**Scope 目錄(Scope catalog)**:
對外(OAuth)的粗粒度授權詞彙,分兩族:組織資料 scope 與會員資料 scope;每個 scope 在內部翻譯為一組權限 key。屬種子資料;OAuth 延後(ADR-0006),目前尚未建。

**模組(Module)**:
後台的頁面劃分單位,形成樹(父模組);對應側欄項目與路由,也是權限的命名空間。側欄類型分三種:可展開群組(非連結)、模組連結、隱藏(僅能由連結進入 — 如編輯頁)。停用父模組時整棵子樹視同停用。

**權限 key 命名**:
key = 擁有模組 key + `.` + 動作(**動作恆為單段**,`*` 是唯一特殊動作),**全小寫 kebab-case、以「.」分層,無例外**,如 `system.user-manager.edit`、`demo.sub.sample-one.edit-page.show-history`。細至欄位顯示都可以是一筆權限。
_Avoid_: camelCase 動作

**權限(Permission)**:
隸屬於某模組的最小授權單位(`moduleId` 指向擁有模組),本質是**頁面裡的按鈕/欄位/跳窗/flag**;同時控制對應 API。key = 擁有模組 key + `.` + 動作(如 `system.user-manager.edit`)。

**功能權限**:
「權限」在與資料範圍對舉時的稱呼,不是另一種資料 — 強調權限只管功能(按鈕、欄位、跳窗、API),看得到哪些資料另由資料範圍規則決定。
_Avoid_: 操作權限、頁面權限

**欄位級權限**:
控制「某一個欄位看不看得到 / 改不改得動」的權限。欄位跨頁共用時**綁父模組**(`docs/concepts/authorization.md`「權限綁在哪一頁」)。沒有 `show-` 那一筆時 api **不把這個欄位放進回傳物件**(不是回空值 — 「沒權限」與「沒填」在值上長得一樣,所以前端依權限集決定渲染,不拿值去猜);欄位**一出現在 input 裡就要** `edit-` 那一筆。配套是變更歷程一律記 `[redacted]`、清單關鍵字不比對它。示範:`demo.sub.sample-one.show-internal-note` / `edit-internal-note`。
_Avoid_: 欄位權限、資料欄權限

**頁面自有權限**:
只在某一頁生效、綁在**那一頁自己的模組**上的權限(隱藏頁最常見),控制的是那一頁的某個**區塊**,而不是整個模組的功能。與該頁的 CRUD **互相獨立** — 少一個區塊不影響新增 / 編輯。示範:`…create-page.show-tips`(填寫提示)、`…edit-page.show-history`(變更歷程)。
_Avoid_: 區塊權限、頁面權限(與「模組 = 頁面」混淆)

**隱藏頁**:
sidebarType=hidden 的模組分兩種:**隱藏頁**(有路由,但不出現在側欄,如新增頁、編輯頁 — 入口是別頁的按鈕;key 一律以 `-page` 結尾)與**權限容器**(無路由、不是頁面,只用來歸屬一組權限,如 `api` 樹、根組織專屬動作的 `tenant-ops`;key 不以 `-page` 結尾)。兩者都可擁有自己的權限。
_Avoid_: 隱藏模組、子頁面

**資料目標**:
資料範圍規則的作用單位,對應**一個模組在一張表上的資料**(以 `(collection, moduleKey)` 識別;所有表單模組共用 `form_submissions`,各自一個目標);由模組 seed 宣告(名稱、描述、可篩欄位),「資料範圍」頁左側一列一個模組。
_Avoid_: 資料集、目標表

**套用對象**:
一條資料範圍規則「套在誰身上」:全部人 / 指定角色 / 指定組織 / 指定使用者。比對的是操作者的**啟用中角色**與**所屬組織的直接關聯**(不是可見範圍、也不是管理範圍);沒有任何規則的套用對象命中他 → 這個資料目標對他不過濾。與「過濾條件」(看得到哪些資料)是一條規則的兩半。
_Avoid_: 適用對象、受眾、audience(中文文案中)

**資料範圍規則**:
決定查詢某資料目標(collection)時**看得到哪些資料的上限**。與權限體系分離(不是權限 key):每個資料目標一份規則設定 — 規則 = 套用對象(全部人/角色/組織/使用者)+ 過濾條件(巢狀 AND/OR),支援動態值(【操作者本人】【操作者的所屬組織】);沒有規則命中 → 預設可見範圍內。由 BaseRepository 執行,租戶隔離保底恆在最外層。是上限不是篩選 — 頁面內的組織下拉等篩選在上限之內運作。
_Avoid_: 資料範圍檔位、資料權限、data scope(中文文案中)

**權限矩陣**:
角色管理「權限設定」分頁的勾選介面。依模組層級顯示模組樹(模組粗體、可勾選 — 勾模組=給路由;勾下層必連動上層),每個模組下縮排列出其權限勾選框(含「全部(*)」,與同層權限互斥連動)。回答「這個**角色**能進哪些頁、頁裡能用什麼」;操作者只能勾自己也有的模組與權限(防越權)。與「權限清單」是兩回事。
_Avoid_: 權限表格、權限清單

**權限清單**:
「模組與權限」頁右側的唯讀區塊:列出選中**模組**定義了哪些權限(名稱、key、kind 標籤)。回答「這個**模組**有哪些權限」,內容為種子資料、只能查閱不能修改;讀者是系統管理員。
_Avoid_: 權限矩陣、權限列表

**萬用字元權限(Wildcard)**:
每個模組固定有一筆的特殊權限(key 為 `模組key.*`,群組、隱藏頁、api 樹皆有)。角色綁定後,**該模組自己這一層**的全部權限 — 包含未來新增的 — 視為擁有;**不涵蓋子模組**,子模組由它自己的 `*` 代表。要整組全給就是子樹每個模組各一筆 `*`。權限解析為聯集、純加法,沒有 deny。
_Avoid_: 含子孫的 wildcard、前綴權限

### 設定資料

正本:`docs/concepts/data-layer-and-isolation.md`「種子資料與遷移」、`apps/db-migrator/seeds/`

**欄位類別(Field Category)**:
一組動態選單的母檔,以 key 識別(如「食譜分類」)。
_Avoid_: 字段管理(改用「欄位管理」稱呼此功能模組)

**欄位選項(Field)**:
欄位類別下的一個選項(label + value)。

**種子資料(Seed data)**:
由程式碼定義、以 key 冪等同步到各環境的設定類資料(模組、權限、種子角色、欄位管理)。與業務資料相對 — 業務資料(帳號、食譜)不跨環境搬移,只做整庫快照。

### 資料層(依「屬於誰」分三類)

正本:`docs/concepts/data-layer-and-isolation.md`「三類資料」、`apps/api/src/database/plugins/tenant-scope.plugin.ts`

**租戶資料**:
有 `orgId` 欄位、每筆屬於一個組織的 collection;查詢時受租戶過濾。例:customers、fields(租戶自訂部分)、audit_logs、示範模組資料;orgs 自身以 `_id` 判定可見。
_Avoid_: 租戶表、業務表

**全域資料**:
全租戶共用同一份、由種子維護、不受租戶過濾的 collection。例:modules、permissions、field_categories、data_scope_targets。
_Avoid_: 系統資料、種子表

**模組資料**:
屬於某個業務模組的租戶資料表(schema 開 `tenantScopePlugin({ moduleData: true })`:示範模組的兩張表、`form_submissions`)。多兩個欄位:`moduleKey`(哪個模組的資料)與 `tenantId`(依組織祖先推導的租戶頂層,只當邊界與索引,不決定可見範圍)。資料範圍規則只套模組資料。
_Avoid_: 業務表、模組表

**業務關聯**:
業務域的多對多關聯(`business_relationships`,目前只有 `org_form`:表單分派 / 啟用)。與核心關聯分表;不掛租戶過濾,改以必填的 `tenantId`(租戶頂層)為邊界,存取一律帶它。
_Avoid_: 核心關聯(那是五個底座實體之間的)

**關聯歸屬資料**:
沒有 `orgId` 欄位、歸屬組織走核心關聯的 collection:users(經 org_user,可屬多組織)、roles(經 org_role)。資料層不自動過濾;模組先查關聯得到 id,再查本表。
_Avoid_: 平台級資料(users 適用、roles 不適用,故不採)

### 表單引擎

正本:`docs/concepts/form-engine.md`、`docs/modules/forms.md`、`apps/api/src/forms/`、`packages/domain/src/form/`、`apps/admin/src/components/form-engine/`

**表單模組**:
seed 宣告 `engine: "form"` 的業務模組;骨架(路由、隱藏頁、權限、資料目標)照舊由 seed 宣告,頁面由表單引擎組裝,資料存所有表單模組共用的 `form_submissions`。範例:購物清單。與固定欄位模組(示範模組那種)並存。
_Avoid_: 動態模組、表單頁

**表單**:
一種填報的身分(如「購物清單」「病假單」),全域唯一 key、**建立後不可改**,掛在一個表單模組下。分共用表單與客製表單。
_Avoid_: 表單範本、表單類型

**共用表單**:
平台建立、可分派給多個租戶的表單(`ownerOrgId = null`)。只有站在根組織的人能建、改、分派。

**客製表單**:
租戶以某張共用表單的某一版為基底建的自己的表單(`ownerOrgId` = 租戶頂層、記 `forkedFrom`);只有該租戶看得到(平台也看不到)。建立時自動在本租戶啟用。
_Avoid_: 私有表單、副本表單(副本專指租戶管理員角色的租戶副本)

**表單版本**:
表單在某時點的欄位、版面、摘要槽、帶入規則。狀態:草稿(可改,同時最多一份)→ 發布中 → 已發布(凍結、供新增,同時最多一份)→ 已退役。提交綁版本,改版不影響舊單。
_Avoid_: 表單修訂(修訂號專指提交)

**欄位(表單)**:
版本裡的一個輸入項(`FieldDef`):key、顯示名、型別(決定存什麼)、widget(決定怎麼畫)、值來源、選項、規則、顯示 / 唯讀條件、欄位級權限。與「欄位管理」的欄位選項是兩回事。

**受保護欄位**:
`permission.show` 為真的表單欄位:要有該欄位的 `show-<formKey>-<fieldKey>` 權限才看得到;看不到時 api 回 `"[redacted]"`。
_Avoid_: 隱藏欄位(那是顯示條件算出來的)

**計算欄位**:
值由公式從其他欄位算出、使用者不填的欄位(`valueSource.kind = "computed"`);送出時後端重算並以後端為準。引用了受保護欄位的計算欄位**自動視為受保護**(沿依賴鏈)。

**表達式**:
公式與條件,存 JSONLogic 樹;設計器以結構化選擇器(欄位 / 運算 / 常數)產生,不做文字輸入。`var` 讀語意值與 `ctx.*`。
_Avoid_: 公式字串、腳本

**摘要槽**:
每版必對的系統槽 `title`,選對的 `date`、`amount`;送出時算成快照,列表、頁籤、搜尋都讀它。受保護欄位不能當摘要槽。

**帶入**:
填寫時從 lookup 來源複製值進欄位,**一次性、之後可改**,不記錄來源;填寫者沒有 edit 資格的欄位不帶。
_Avoid_: 自動填入、引用(要追溯來源用引用)

**引用**:
`reference` 型別欄位:存來源 id + 送出當時的顯示名快照;顯示時來源還在且讀者有權就顯示現名,否則顯示快照 +「(來源不可用)」。

**lookup 來源**:
可被帶入、引用或當選項的資料來源(使用者、組織、其他表單的提交),程式內登錄;查詢時 provider 與固定條件一律從版本定義取,前端只帶「哪個版本的哪個欄位」+ 關鍵字。
_Avoid_: 資料來源(太泛)

**表單分派**:
平台決定一張共用表單開放給哪些租戶(業務關聯 `org_form`);收回 = 刪這筆,已存在的提交照常可看。

**表單啟用**:
租戶決定分派來的或自己的表單在本租戶開不開(`org_form.meta.enabled`,預設開)。停用只擋新增。

**列表欄位配置**:
表單模組列表顯示哪些欄(摘要槽或表單欄位)、順序與寬度(`modules.settings.list`),平台設定全域預設;引用到那一筆的版本沒有的欄位時顯示「—」。

**提交**:
使用者填的一筆資料(`form_submissions`),綁表單版本、屬於一個表單模組。草稿(存了沒送、只屬於建立者)→ 送出:沒綁流程的即已完成、可再修改;綁了流程的進審核(狀態七值見「審核流程」)。
_Avoid_: 表單資料、申請單(申請中心的「申請」指的就是提交,不另立名詞)

**修訂號**:
已完成的提交每改一次 +1;每個修訂號都留完整值快照與當次的 `ctx`(時間、時區、操作者、當時組織),差異由相鄰兩筆算。
_Avoid_: 版本(版本專指表單版本)

### 審核流程

正本:`docs/concepts/workflow-engine.md`、`docs/modules/workflows.md`、`apps/api/src/workflows/`、`packages/domain/src/workflow/`、`apps/admin/src/components/workflow/`

**流程**:
一組審核關卡的身分(如「請假審核」),全域唯一 key、**建立後不可改**。和表單一樣是執行期資料(畫面上設計、版本化、發布),不寫進 seed。分共用流程與客製流程。
_Avoid_: 簽核單、工作流範本

**共用流程**:
平台建立、可分派給多個租戶的流程(`ownerOrgId = null`)。不能指定使用者、角色關卡只存佔位,所以含角色的共用流程租戶不能直接綁,要以它為基底建客製流程。

**客製流程**:
租戶自建、或以某個流程某一版為基底建的自己的流程(`ownerOrgId` = 租戶頂層、以基底建的記 `forkedFrom`);只有該租戶看得到,建立時自動分派給自己。
_Avoid_: 私有流程

**流程版本**:
流程在某時點的關卡定義(`steps` + 選填的 `edges`);生命週期與表單版本同一套(草稿 → 發布中 → 已發布 → 已退役,四步發布、冪等重試)。實例記自己跑的是哪一版,改版 / 退役只影響新送出。
_Avoid_: 流程修訂

**關卡**:
版本裡的一個節點,穩定 `key`、發布後不可改。兩種:審核關卡(誰審、會簽模式、跳過條件、可否退回)與匯合節點(系統節點,不派人)。同一實例內每關最多執行一次。
_Avoid_: 節點(只在講流程圖時用)、階段

**分流 / 匯合**:
一個審核關卡完成後同時啟動多條分支 = 分流;分支最後都接到同一個**匯合節點**,它等全部分支通過(或依條件跳過)才自動完成,之後可結束或接審核關卡。分流與匯合明確配對,不巢狀、不交叉。
_Avoid_: 並簽、條件分支(條件分支不做)

**會簽模式**:
關卡內幾個人要過:`any`(一人核准即過、一人駁回即駁回)/ `all`(全部核准才過、一人駁回即駁回)。和匯合(跨分支、固定全部)是兩件事:財務關 `any` 不能代替法務關的審核。
_Avoid_: 會簽 vs 匯合混用

**審核者來源**:
關卡找人的方式,四種:指定使用者、角色、表單欄位(使用者引用欄)、主管。只在進到該關時解析一次,寫進派任計畫後不再重算。

**主管**:
組織的主管使用者(`org_manager`,一個組織可多位),在組織管理設定。申請人的主管 = 從**提交所屬組織**往上找到的第一組主管;第 2 層再往上一組。申請人自己不審自己。
_Avoid_: 上司、直屬長官(UI 可寫「直屬主管」當關卡名稱,模型一律「主管」)

**流程分派**:
平台決定一個共用流程開放給哪些租戶(業務關聯 `org_workflow`);收回後該租戶的新送出會被擋,進行中的審核照常走完。

**流程綁定**:
某租戶的某張表單送出後走哪個流程(業務關聯 `org_form_workflow`);沒有這筆 = 不走流程。一張表單最多綁一個流程,多張表單可綁同一個。綁定指向被收回或不存在的流程 = 「綁定的流程已失效」,送出一律擋。
_Avoid_: 流程設定、掛流程

**流程實例**:
一筆提交的某個修訂號送出後,依綁定的流程版本產生的一次執行;一筆提交同時最多一個進行中。**實例是唯一權威**:關卡進度、派任計畫、被接受的決定都記在實例上。
_Avoid_: 簽核單、案件

**派任計畫**:
進關時一次算好、寫進實例的「這一關派給誰」清單,每項有不變的 `taskKey`;任務依它建、依它改派。

**審核任務**:
實例在某關對某個審核者產生的待辦,是派任計畫一項的投影(任務文件本身不是權威)。簡稱任務。
_Avoid_: 待辦(UI 可寫「待我審核」)、簽核

**審核決定**:
審核者對任務的動作:核准、駁回(理由必填)、退回修改(理由必填,該關要允許退回)。原子地記進實例才算被接受;同一關依接受順序算結果,晚到的決定記成「晚於結果」不影響。
_Avoid_: 批示、簽核意見

**阻擋**:
關卡解析不到有效審核者、或審核者失效(停用 / 移出租戶)時停住,等流程管理者處理;**不自動改派、不自動跳過**。其他分支照常審。
_Avoid_: 卡單、例外

**改派**:
流程管理者把任務指給另一個人(`taskKey` 不變,原承辦人記進「曾持有」),或對解析為空的關卡新增審核者。在阻擋清單操作。
_Avoid_: 轉簽、代理人(代理人不做)

**推進**:
引擎的冪等動作:看實例目前狀態把該做的做完(建任務、關卡完成、下一關、結束、收尾);任何入口重跑結果相同。流程管理者可按「重試推進」。

**撤回**:
申請人在還沒有任何被接受的決定前把實例收回,提交回到可修改。

**作廢**:
已核准的提交被標記不再生效,內容凍結;可「複製為新單」重送。申請人可自行作廢,不需主管同意。
_Avoid_: 刪除、取消(取消專指任務因關卡結束而不用再審)

**申請中心**:
固定模組(`apply-center`):「我的申請」「待我審核」兩個跨模組頁籤 + 「新申請」入口 + 自己的詳情頁;表單仍留在各自的業務模組。
_Avoid_: 簽核中心、待辦中心

**流程管理**:
固定模組(`system.workflows`):流程清單、設計器、版本面板、分派、以此為基底建流程,以及阻擋清單(改派 / 新增審核者 / 重試推進)。
