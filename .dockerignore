# 依賴與建置產物(容器內會重新安裝/建置)
node_modules
**/node_modules
**/dist
**/.next
**/.turbo
**/storybook-static
**/coverage

# 與執行無關
.git
.github
.claude
.agents
docs
scripts
**/*.md
# 例外:admin 的模組說明是程式資產(Vite 在 build 時以 import.meta.glob 內嵌進 bundle),
# 不是文件 —— 排在 `**/*.md` 之後才生效(dockerignore 後出現的規則覆蓋先前的)。
!apps/admin/src/md/**/*.md

# 機密
.env
**/.env
**/.env.*
!**/.env.example
